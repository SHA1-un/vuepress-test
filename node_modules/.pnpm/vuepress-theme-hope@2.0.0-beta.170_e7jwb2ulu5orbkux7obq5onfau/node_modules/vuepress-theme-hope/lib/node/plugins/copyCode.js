import { copyCodePlugin } from "vuepress-plugin-copy-code2";
export const getCopyCodePlugin = (options) => {
    if (options === false)
        return null;
    return copyCodePlugin({
        selector: '.theme-hope-content div[class*="language-"] pre',
        ...options,
    });
};
//# sourceMappingURL=copyCode.js.map